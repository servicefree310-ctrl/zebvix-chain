# Overview

This project is a pnpm workspace monorepo for the Zebvix L1 blockchain, developed using TypeScript and Rust. It aims to be a high-performance, secure, scalable, and user-friendly platform featuring a Cancun-EVM-bytecode-compatible execution layer (ZVM), an integrated DeFi AMM (zSwap), robust governance, and seamless cross-chain bridging. Key capabilities include a custom L1 blockchain, an on-chain permissionless zSwap AMM, a comprehensive dashboard, mobile wallet connectivity, and a ZVM for smart contracts with zero-config compatibility with existing Ethereum tooling.

# User Preferences

Communicate in Hinglish with the user.

# System Architecture

## Monorepo Structure
The project is organized as a pnpm workspace monorepo with key packages for the core blockchain, API server, React-based dashboard, Flutter mobile wallet, and a TypeScript SDK.

## Core Technologies
The project utilizes Node.js 24, TypeScript 5.9, Express 5, and Rust for backend development. Data persistence is handled by PostgreSQL (off-chain) with Drizzle ORM and RocksDB (on-chain). Zod is used for validation, Orval for API codegen, and esbuild for the build system.

## Zebvix L1 Blockchain
The `zebvix-chain` is a Rust-based L1 blockchain featuring a 2-node P2P gossip sync consensus, RocksDB-backed validator registry, and Ed25569 votes (secp256k1 for ETH-compatible addresses). It supports various transaction types including `Transfer`, `ValidatorAdd/Remove`, `Swap`, `Bridge`, `Proposal`, and `TokenPool` operations. Tokenomics include a 150M ZBX supply with Bitcoin-style halving. The on-chain zSwap AMM follows `x·y=k` with a 0.3% fee and supports permissionless Uniswap V2-style TOKEN/ZBX pools. The blockchain includes a bridge module for cross-chain operations and a forkless on-chain governance system supporting various proposal types.

## ZVM (Zebvix Virtual Machine) Integration
The ZVM is a native Rust implementation that is Cancun-EVM-bytecode compatible, offering full Cancun opcode coverage and mainnet-matching gas accounting. It uses `CfZvmDb` backed by a RocksDB column family for storage. The system supports real `ZvmReceipt` persistence and log management, cross-domain settlement where `eth_getBalance` and `eth_getTransactionCount` return `max(zvm, native)`, and monetary gas accounting with pre-flight checks and post-frame refunds. Standard Ethereum precompiles are dispatched, and custom Zebvix precompiles handle native side-effects. The JSON-RPC wire protocol adheres to `eth_*` namespace with `zbx_*` aliases.

## User Interface (Dashboard)
The dashboard provides a Mission Control for chain stats, a ZVM Explorer, Pool Explorer, Tokenomics, Smart Contracts, Cross-Chain Bridge, Multisig Wallet Tools, and enhanced Balance Lookup. It includes ZBX Wallet functionality with native transfers, MetaMask integration, transaction tracking, and an "Import Wallet" feature. An Etherscan-style Block Explorer and an RPC Playground are also available. Users can register Pay-IDs for `handle@zbx` aliases and utilize a Staking Dashboard for delegator operations. The dashboard features a typed transaction decoder and allows for permissionless creation of ERC-20-style tokens.

## Zebvix Sites — Web3 Site Builder
A separate product artifact (`artifacts/zsites`, base path `/sites/`) that lets anyone describe a business and get a publishable Web3 site. Stack: Clerk auth, Anthropic (`claude-sonnet-4-5`) AI generator, block-based editor with 13 block types (nav/hero/features/pricing/testimonials/faq/cta/text/image/gallery/lead_form/crypto_checkout/footer), 5 polished templates (SaaS/NFT/Agency/Restaurant/Portfolio), 1-click publish to subdomain (`/sites/p/:subdomain`), built-in crypto checkout (ZBX/zUSD/BNB on Zebvix L1, chainId 7777) verified on-chain via `zbx_getEvmReceipt` with `eth_getTransactionReceipt` fallback, lead capture, and per-site analytics with daily traffic charts. Backend routes mounted under `/api/sites/*` from `artifacts/api-server`; database tables: `sites`, `leads`, `site_payments`, `page_views`.

## BSC ↔ Zebvix Bridge
This bridge replaces a single-admin attestation MVP with an M-of-N validator multisig for Zebvix → BSC mints. On BSC, `WrappedZBX` (ERC20) and `ZebvixBridge` contracts manage minting and burning. Off-chain, a `bridge-relayer` service polls for bridge events and aggregates M-of-N EIP-712 signatures for mints, while a `bridge-signer` daemon independently verifies transactions before signing. The dashboard integrates a `BscSidePanel` and a unified bridge UI for both directions, supporting in-browser secp256k1 key signing. The `api-server` provides endpoints for bridge configuration and relayer status.

## Security Hardening
Security measures include block forgery defense, mempool DoS hardening (balance checks, nonce windows, fee-priority eviction), and RPC security with CORS defaults to localhost-only. Slashing mechanisms are in place for double-signing. A bridge kill-switch (`zbx_bridgePaused`) allows administrators to halt new BridgeIn/BridgeOut flows. Operational hygiene emphasizes validator key file security, daily RocksDB snapshots, and RPC binding restrictions.

### BFT Commit-Gate Pipeline (Phase B.3.2.5, April 2026 — side-table architecture, Phase 2 wired)
The BFT commit gate uses a **side-table** layout, NOT in-block fields. `Block` and `BlockHeader` byte layouts are unchanged from chain inception, so existing RocksDB databases boot without any migration or wipe. BFT commits are persisted at `CF_META` key `bft/c/<32-byte block_hash>` as `bincode::serialize(&Vec<Vote>)`, accessed via `State::put_bft_commit(&block_hash, blob)` and `State::get_bft_commit(&block_hash)`. The verifier `vote::verify_last_commit_for_parent(parent_hash, parent_height, last_commit_bytes, chain_id, validators)` enforces per-vote sanity (chain_id / height / type / target / sig), dedup, validator-set membership, the 2/3+ voting-power quorum, and the genesis-adjacent rule (parent_height==0 → must be empty). The gate is wired into `state::apply_block` after the proposer signature check: it reads `get_bft_commit(parent_hash)` and verifies it when `block.header.height >= ZEBVIX_BFT_COMMIT_GATE_ACTIVATION_HEIGHT` (default `u64::MAX` = OFF — single-validator devnet behavior unchanged).

**Phase 2 wiring (commit persistence):** `main::try_persist_bft_commit_for(state, pool, height, target_hash)` runs from BOTH vote-handling paths (the local emit task that signs Prevote+Precommit on every tip-advance, and the p2p inbound vote handler) on every `AddVoteResult::Inserted { reached_quorum: true }` for a Precommit. It calls `pool.collect_precommits_for(...)` (deterministic via sort by validator address, aggregates across all rounds), bincodes the `Vec<Vote>`, and writes via `put_bft_commit`. Logged at INFO as `📜 BFT commit persisted h=N hash=0x... precommits=K bytes=B` — operators grep this to confirm the pipeline is live. The local emit task initializes `last_emitted = tip.saturating_sub(1)` so a node restart re-emits + re-persists the commit for the existing tip block on the very first tick (idempotent: `pool.add` returns `Duplicate` for re-signed votes, RocksDB put overwrites with the same deterministic bytes).

**Phase 3 ops procedure (flip the gate ON):** Once the Phase B.3.2.5 binary is deployed and `📜 BFT commit persisted` is appearing on every block, the operator computes `ACTIVATION = current_tip + 100` (≈8min buffer at 5s/block), runs `sudo systemctl edit zebvix` to add `Environment="ZEBVIX_BFT_COMMIT_GATE_ACTIVATION_HEIGHT=NNNN"`, and restarts. After height passes ACTIVATION, every `apply_block` enforces that the parent's commit blob exists and verifies. Honest single-validator: trivially passes (1/1 quorum). Multi-validator: requires 2/3+ Precommit power on the parent.

**Honest risk profile:** 11 isolated unit tests in `vote.rs::tests` cover the verifier surface. **DB compatibility:** `EXPECTED_DB_FORMAT_VERSION = 1`; legacy DBs without the marker are auto-stamped v1 on first boot (logged at INFO) — no wipe required. With N=1 validator the gate adds no security improvement over PoA (the lone signer IS the quorum); the gate only hardens N≥4. See `HARDENING_TODO.md` C1 for the full status table.

### Phase B.3.2.6 — Tendermint-style FSM Module (April 2026, runtime integration deferred)
A pure-function consensus FSM ships at `zebvix-chain/src/fsm.rs` (~1200 lines including tests, 20 green tests). It models a single height as a sequence of rounds, each cycling Propose → Prevote → Precommit → Commit, with full lock-on-precommit + POL-release semantics, valid-block tracking for proposer convergence, view-change via timeouts (propose/prevote/precommit each have configurable durations) and via observing `f+1` votes at any round greater than the local round. Late precommit quorums from past rounds still commit the block. The module is intentionally **dead code** in this release: `lib.rs` exports it, `cargo check` compiles cleanly, but `Producer::run` is unchanged and the live VPS chain at h=50K+ continues to produce blocks via the legacy PoA path. The next session will gate the swap-in behind a new env flag `ZEBVIX_FSM_ENABLED` (default OFF) so existing operators are not auto-upgraded; with the flag ON under N=1 the FSM walks every step trivially (1/1 quorum is always met) and produces byte-identical blocks; with N≥2 it delivers real BFT safety + liveness. Tests run via standalone `rustc --test` because the workspace's full `cargo test --lib` rebuild of `librocksdb-sys` exceeds the dev-environment's per-command CPU budget.

**Architect-review safety hardening:** the first review pass returned a FAIL verdict with three critical findings, all addressed in the final shipped module: (1) **height binding** — every `FsmEvent` (except `Tick`) now carries an explicit `height: u64`, and `FsmState::step` silently drops events whose height ≠ `self.height` so misrouted vote-pool signals can never commit the wrong height's block; (2) **precommit-only-on-seen-proposal** — non-nil precommit fires only when `self.proposal == Some(quorum_target)`, otherwise nil, so a hash-only quorum from byzantine peers (or a buggy plumbing layer) cannot forge our signature on data we never validated; (3) **apply-acknowledged height advance** — `Step::Commit` no longer auto-advances on commit-timeout; the runtime must send a new `FsmEvent::BlockApplied { height, hash }` event after a successful state apply + side-table commit-blob persist, and only then does `enter_height` fire. Mismatched-hash acks are refused. A new `committing: Option<Hash>` field on `FsmState` tracks the in-flight commit and also blocks any nil-quorum round-bump while the apply is outstanding. Five new safety tests cover these paths (height-mismatch silent-drop, precommit-nil-on-unseen-hash, precommit-non-nil-after-proposal-seen, BlockApplied-wrong-hash-ignored, no-round-bump-while-committing).

### F006.5 + H1 ship batch (April 28, 2026)
A two-in-one batch landed in `zebvix-chain` immediately following the F006.4 LIVE deploy (chain at h≈54980+) — pairing the next planned F006 sub-feature with the highest-priority finding from the post-F006.4 security audit, both as defense-in-depth additions with zero new dependencies. **(F006.5) Cadence rate-limiter backstop (`zebvix-chain/src/fsm_runtime.rs`, shipped DORMANT).** New `MIN_BLOCK_INTERVAL: Duration` const compile-time tied to `crate::tokenomics::BLOCK_TIME_SECS` (currently 5s) so any future tokenomics retune propagates automatically — enforced by a `min_block_interval_matches_block_time_secs` parity test that fails CI if the two ever drift. New field `last_commit_at: Mutex<Option<Instant>>` (renamed from the F006.6-stub `last_proposal_at: Mutex<Instant>`; defaults to `None` so the genesis-bootstrap commit is never rate-limited, set to `Some(Instant::now())` only AFTER a successful `state.apply_block` so a failed apply never "consumes" a cadence slot). Defense-in-depth gate inside `handle_action(FsmAction::CommitBlock { ... })` placed AFTER the existing tip short-circuit but BEFORE `proposal_cache.lock().await.remove(&height)` — when elapsed time since the previous successful apply is below `MIN_BLOCK_INTERVAL`, the dispatch returns `Ok(Vec::new())` (FSM stays in `Step::Commit`, next `tick_once` re-issues the same `CommitBlock`) and the cached proposal is left untouched so the retry hits the same block bytes. **Why apply-site, not BuildProposal?** (1) `BuildProposal` is a no-op stub in F006.4 shadow-observer mode — no cadence to limit yet. (2) Gating at the apply site is what actually matters for byte-identical commit blobs (timestamp is set at proposal time, but the on-chain effect of cadence is the spacing between `state.apply_block` calls). (3) F006.7 will additionally raise `Timeouts.commit` to `BLOCK_TIME_SECS` making this backstop redundant in the happy path — it stays as defense-in-depth against future misconfiguration. Three new tests bring the chain test count to **201 total** (was 198 after F006.4): the parity guard above plus `handle_action_commit_block_rate_limited_returns_empty_and_preserves_cache` (proves the gate fires BEFORE cache mutation by setting `last_commit_at = Some(now)` and verifying both empty-events return AND cache preservation) and `handle_action_commit_block_rate_limit_clears_after_min_interval` (sets `last_commit_at` to `MIN_BLOCK_INTERVAL + 50ms` ago via `Instant::now().checked_sub(...)` and verifies dispatch reaches the cache-miss error path, proving elapsed-time math is correct). F006.5 is shipped DORMANT — `enabled()` still defaults `false`, the gate only fires when the FSM is actually driving commits (activated under F006.7); live VPS chain at h≈54980+ continues unaffected. **(H1) In-process RPC concurrency cap (`zebvix-chain/src/rpc.rs`, ACTIVE on next deploy).** Identified in the post-F006.4 security audit as the single remaining "no in-process backstop" risk: a misconfigured or absent nginx/Cloudflare front could let an RPC flood saturate the tokio runtime and starve the consensus producer task — a liveness-degrading attack that would not corrupt state but would bring block production to a halt. Fix: new module-level `pub(crate) const RPC_DEFAULT_MAX_INFLIGHT: usize = 256`, `pub(crate) static RPC_INFLIGHT_SEMAPHORE: once_cell::sync::Lazy<std::sync::Arc<tokio::sync::Semaphore>>` tunable at startup via `ZEBVIX_RPC_MAX_INFLIGHT=<n>` env var (uses `once_cell` + `tokio` — both already in workspace deps, **zero new crates** in Cargo.toml), and `pub(crate) async fn rpc_concurrency_limit_middleware(req, next) -> Response` that calls `RPC_INFLIGHT_SEMAPHORE.clone().try_acquire_owned()` (NEVER `acquire_owned().await` — queueing on overflow would defeat the purpose by holding tokio tasks open and starving consensus) and returns HTTP 503 + plain-text "too many concurrent requests, please retry shortly" body on exhaustion, with a `tracing::warn!` audit-log line so operators can spot saturation patterns in the journal. Wired into the existing `pub fn router(ctx) -> Router` chain via `.layer(axum::middleware::from_fn(rpc_concurrency_limit_middleware))` placed between `.with_state(ctx)` and `.layer(cors)`, so layer order is **cors (outermost) → concurrency_limit → body_limit → handler** — concurrency check fires BEFORE body parsing (rejects 503 without reading payload bytes) but AFTER CORS preflight (cheap browser handshakes never consume permits). `once_cell::sync::Lazy::force(&RPC_INFLIGHT_SEMAPHORE)` called at the top of `router()` so the configured cap is logged at startup, not on first request — any env-parsing surprise surfaces in the boot log before traffic arrives. **Why a global semaphore rather than per-IP token bucket?** (1) Per-IP needs IP extraction from the proxied connection (X-Forwarded-For parsing is fragile across nginx/Cloudflare/Cloudflare-tunnel topologies) and a GC'd HashMap of buckets — both reasonable to defer to the out-of-process limiter that already does them well. (2) The single risk this layer must defeat is "the producer task misses a tick deadline because tokio's runtime is saturated with cheap RPC reads" — a global concurrency cap is the minimal mechanism that does that. (3) Per-IP rate limiting remains delegated to nginx / Cloudflare in front of the public RPC (out-of-process limiters survive node restarts and can share state across multiple RPC nodes; the in-process semaphore here is a global cap, not per-IP). Existing RPC-side defenses unchanged: 256 KiB body limit, mempool fee floor (`MIN_TX_FEE_WEI`), mempool max_size (50 000), per-block tx cap (`MAX_TXS_PER_BLOCK = 5 000`). H1 is **ACTIVE on next deploy** — no flag to flip, no height gate, pure defense-in-depth that can only reject excess load and never produce wrong state. **Outstanding audit follow-ups (operator-side, not code):** rotate `ADMIN_TOKEN` (M1) and add nginx `limit_req` per-IP budget in front of the RPC (M3) — both still pending. **Dev-environment limitation:** the chain crate cannot be `cargo check`ed in the dev environment because `librocksdb-sys` rebuild exceeds the per-command CPU budget; operator must run `bash scripts/ci-check.sh` on the build host before pulling the tarball — D10's stale-check blocks accidental un-validated streams. **Ship-table updates** in `zebvix-chain/HARDENING_TODO.md`: F006.5 row → "✅ shipped dormant ⁵", D9 row expanded to "D9 / H1" with status "🟡 H1 concurrency cap shipped 2026-04-28 ⁵; pagination + per-method range caps ⏳", new footnote ⁵ documenting both halves of the batch.

### Phase E.0 — Sui/Aptos/Solana-tier Testnet bootstrap infrastructure (April 28, 2026)
A new top-level **Phase E** roadmap (~31 sessions, ~6 months) ships in `zebvix-chain` to evolve the chain into a Sui/Aptos/Solana-tier high-security L1 — but **on a parallel testnet, not the live mainnet**. The user's existing live VPS chain at h≈55808+ stays 100% untouched while every chain-breaking change (F006.7 FSM activation, D1 multi-validator, D2 slashing, C2 Keccak signing, C3 bridge multisig, H2 Block-STM parallel exec, D7 EIP-1559 fee market) lands on testnet first, soaks for 30+ days, then height-gate-activates on mainnet via the existing playbook. Phase E.0 (this ship batch) is the **bootstrap infrastructure** — a single Cargo feature flag + two VPS scripts + loud banner safety. **(1) Cargo `testnet` feature (`zebvix-chain/Cargo.toml`).** Zero new deps, additive `[features]` entry. Build with `cargo build --release --features zvm,testnet` to produce a testnet binary; default build (no feature) produces the mainnet binary with byte-identical behaviour to the live VPS. **(2) Compile-time gated tokenomics (`zebvix-chain/src/tokenomics.rs`).** `CHAIN_ID` flips from 7878 (mainnet) → 78787 (testnet) so signed transactions cannot replay across networks. **Architect-found bug, fixed mid-Phase-E.0:** the original ship claimed cross-chain replay was "blocked by construction" via signed body bytes, but in fact NEITHER `Mempool::add` NOR `State::apply_tx` ever compared `tx.body.chain_id` against `tokenomics::CHAIN_ID` — meaning a mainnet-signed tx could be accepted by a testnet node (and vice versa) any time nonce/balance happened to align. The fix introduces a new public const `tokenomics::ENFORCE_CHAIN_ID: bool` (testnet=`true`, mainnet=`false`) that gates a runtime check at both admission and apply paths: when enabled, any tx whose `body.chain_id != CHAIN_ID` is rejected with a loud `wrong chain_id: tx signed for X, this network is Y (NETWORK_NAME)` error BEFORE state mutation, fee debit, or nonce bump. The mempool check sits between fee floor and secp256k1 verify so wrong-chain spam costs only one u64 comparison. Mainnet enforcement is intentionally deferred to a future height-gated Tier 1 phase coordinated with the C2 Keccak migration: mainnet has 55808+ blocks of unaudited history that pre-date this gate, so genesis activation would risk state-sync divergence on any historical tx that happened to carry `body.chain_id != 7878`. **Architect follow-up review caught a second gap:** with the gate ONLY on `apply_tx`, a block containing a wrong-chain tx would still be rejected (replay safety intact) BUT the rejection would fire inside the runtime apply loop AFTER `set_block_applying_marker(...)` — triggering the fail-loud "leave marker stuck" path and bricking the node until manual operator recovery (DoS via crafted block). Fix: an additional pre-check sits at the TOP of `apply_block`, BEFORE the marker is touched, that walks `block.txs` and short-circuits the whole block with a loud error if any tx carries a wrong chain_id. Four new regression tests in `state.rs` lock the behaviour down: an always-on parity guard (`CHAIN_ID` and `ENFORCE_CHAIN_ID` must move together with the build flavor), plus three testnet-gated rejection tests covering `Mempool::add`, `State::apply_tx`, AND `State::apply_block` — the apply_block test explicitly asserts `read_block_applying_marker().is_none()` after rejection so any future regression that moves the pre-check past the marker fires this test immediately. Eight existing apply_tx test fixtures that hardcoded `chain_id: 7878` were lifted to `chain_id: crate::tokenomics::CHAIN_ID` so the suite stays green under both build flavors. The BFT vote and evidence layers (`vote.rs:148`, `vote.rs:481`, `evidence.rs:146`) already enforced chain_id at runtime, so this fix closes the matching transaction-layer gap. `FOUNDER_PREMINE_ZBX` flips from 9.99M (real economic value) → 100M (zero economic value, flat ops convenience to fund validator-add txs and load tests during multi-month soak). All other tokenomics constants — `FOUNDER_PUBKEY_HEX`, `ADMIN_ADDRESS_HEX`, `GOVERNOR_ADDRESS_HEX`, `BLOCK_TIME_SECS`, halving schedule — stay identical so testnet faithfully simulates mainnet economics; only network identity and starting balance differ. New public `const fn network_name() -> &'static str` and `const fn is_testnet() -> bool` exposed for use in banners, logs, and runtime checks (e.g. faucet RPC gated open on testnet only). **(3) Loud banner identifier (`zebvix-chain/src/main.rs`).** `print_banner()` updated to display network name on a dedicated line: green `✓ MAINNET` for mainnet builds, red `🧪 TESTNET 🧪` (with bold ANSI 1;31m) plus a one-line warning below the box for testnet builds. `pad_visible` helper added because emoji-containing strings have ambiguous `.chars().count()` width; visible widths are hard-coded for the two known label variants (15 cols for testnet, 10 for mainnet). Operator can never confuse the two binaries — the banner shouts the network identity at every CLI invocation. **(4) `scripts/testnet-deploy.sh`.** Single-script VPS testnet bootstrap with four modes (`--build-only`, `--service-only`, `--status`, default = full). Builds with `--features zvm,testnet`, atomic `install -m 755` to `/usr/local/bin/zebvix-node-testnet` (handles ETXTBSY when binary is already running, lesson learned from F006.5+H1 deploy), writes `zebvix-testnet.service` systemd unit (RPC `0.0.0.0:18545`, P2P `31333`, home `/root/.zebvix-testnet`, `ZEBVIX_RPC_MAX_INFLIGHT=128` for testnet's lower budget), restarts service with 3s settle, verifies TESTNET banner appears in journal. `--status` mode is a quick health-check that shows service status + last 30s of journal + tip via local RPC. **(5) `scripts/testnet-genesis-keygen.sh`.** Wrapper around `zebvix-node-testnet keygen` for per-node validator keypair generation. Restricts output dir to `0700` and key file to `0600`; refuses to overwrite existing keys; prints next-step commands for `validator-add` tx submission to expand the validator set post-genesis. **Surface allocation (mainnet vs testnet on the same VPS):** binary `/usr/local/bin/zebvix-node` vs `…-testnet`; systemd `zebvix.service` vs `zebvix-testnet.service`; RocksDB `/root/.zebvix` vs `/root/.zebvix-testnet`; RPC `:8545` vs `:18545`; P2P `:30333` vs `:31333`. Zero collisions. Source tree is shared (`/home/zebvix-chain`) so a single `git pull` updates both binaries on rebuild. **Phase E roadmap (next 12 sessions):** Phase 1 = F006.7 FSM wire-in (BFT activation, testnet first), Phase 2 = D5 backup/DR + Shamir validator key sharding (single biggest residual risk today), Phase 3 = D1 multi-validator deploy (N=3 → N=20 progression), Phase 4 = D2 slashing jail/burn execution, Phase 5 = H5 gossipsub peer scoring, Phase 6 = C3 bridge multisig (Tier-1 highest priority after F006), Phase 7 = C2 Keccak signing migration (MetaMask compat), Phase 8 = H2 Block-STM parallel execution (Aptos-style, ~5 sessions, largest single item), Phase 9 = D3 state-sync snapshots, Phase 10 = D7 EIP-1559 fee market, Phase 11 = D6 explorer parity, Phase 12 = DR drills + 30-day soak + mainnet swap planning. **Mainnet untouched throughout** — every cycle ends with the testnet validating the change end-to-end before the activation height ever gets set on mainnet. **Self-review confirmed mainnet path byte-identical:** `git diff --stat` shows 114 insertions / 2 deletions; the 2 "deletions" are inline mainnet const values being relocated into `#[cfg(not(feature = "testnet"))]` blocks with bit-identical right-hand sides. Mainnet build (no feature) produces `tokenomics::CHAIN_ID = 7878` and `FOUNDER_PREMINE_ZBX = 9_990_000` exactly as before. Live VPS at h=55808+ requires no action — the next mainnet rebuild will pick up the additive testnet infrastructure but emit a binary with identical mainnet behaviour. **Operator next step (when ready):** `sudo bash scripts/testnet-deploy.sh` on the VPS to bring up testnet alongside mainnet. After that, Phase 1 (F006.7 wire-in) can land on testnet without any mainnet risk.

### F006.5 + H1 LIVE deploy verification (April 28, 2026)
The F006.5 + H1 ship batch landed on production VPS srv1266996.hstgr.cloud at 02:22:30 UTC after a four-step deploy: (1) tarball pulled via `curl -fsSL .../api/download/newchain?force=1` (force-bypass needed because dev workspace cannot refresh `target/.last-ci-pass` due to librocksdb-sys CPU budget — bypass logs warn audit on api-server side), (2) extracted in-place over `/home/zebvix-chain` (target/ preserved for warm RocksDB cache → 1m 39s rebuild instead of 8min cold), (3) full ci-check on VPS (cargo check + clippy -D warnings + cargo test --lib --features zvm → 201/201 PASS, marker refreshed), (4) `cargo build --release --features zvm` produced 16 MB stripped binary at `target/release/zebvix-node`. **Two operator-side traps discovered + remediated for future deploys.** First, the binary path: `Cargo.toml [package].name = "zebvix-node"` (not `zebvix-chain`) so cargo emits `target/release/zebvix-node`; the systemd ExecStart points at `/usr/local/bin/zebvix-node` (the install path, not the build path), so `systemctl restart zebvix` alone runs the OLD binary unless a copy step intervenes. Second, `cp target/release/zebvix-node /usr/local/bin/zebvix-node` on a running binary fails with **ETXTBSY ("Text file busy")** because Linux refuses to truncate a currently-mapped executable; the correct command is `sudo install -m 755 <src> <dst>` (or `sudo mv`) which `unlink()`s the target first → kernel keeps the old inode mapped for the running process while the new content goes to a fresh inode at the same path → next `systemctl restart` picks up the new binary cleanly with zero downtime gap. **Verification (post-`install` + restart):** SHAs match (`1e9f80ac...` on both `/usr/local/bin/zebvix-node` and `target/release/zebvix-node`); journal at 02:22:30 UTC shows the H1 boot banner `🛡  RPC in-process concurrency cap: max 256 in-flight requests (set ZEBVIX_RPC_MAX_INFLIGHT to tune; per-IP limiting still recommended via nginx / Cloudflare in front)` immediately followed by the existing CORS banner — confirming both `Lazy::force(&RPC_INFLIGHT_SEMAPHORE)` at `router()` startup and the M-fix `RPC_EFFECTIVE_MAX_INFLIGHT` atomic store fire correctly under tracing subscriber. Tip advanced 55701 → 55808 across the deploy window (107 blocks in ~10min ≈ 5.6s/block — perfect cadence, zero missed blocks across two restart events). **F006.5 dormancy confirmed** — zero `fsm_runtime` log lines (FSM not constructed in `main.rs` yet, gate code present but unreachable until F006.7 wires the runtime adapter into `Producer::run` behind `ZEBVIX_FSM_ENABLED=true`). **Outstanding ops items (deferred, separate from this code ship):** M1 ADMIN_TOKEN rotation, M3 nginx `limit_req` per-IP budget in front of `:8545`. **Architect-deferred follow-ups:** RPC test for middleware-ordering / 503-on-saturation regression guard, F006.7 plan documenting tip-check/apply TOCTOU race acceptance during legacy → FSM cutover. **Lesson hardcoded for next deploy:** the on-VPS deploy script must use `sudo install -m 755 target/release/zebvix-node /usr/local/bin/zebvix-node` between `cargo build --release` and `systemctl restart zebvix` — otherwise the restart cycles the OLD binary silently (everything looks healthy, just the new code never executes).

### Phase D / F006 max-batch ship #2 (April 27, 2026)
A second `zebvix-chain` ship batch landed advancing four roadmap items from partial (🟡) or planned (⏳) to fully shipped dormant (🟢 / ✅), each gated to remain inert on the live VPS chain (h≈52000) until explicit operator opt-in. **(D10) Tarball-handler stale-check (`artifacts/api-server/src/routes/download.ts`).** `streamFreshTar` now stat's `zebvix-chain/target/.last-ci-pass` and refuses to stream when the marker is absent OR > 24h old, returning 503 with a structured `{ ok: false, gate_reason, remediation }` JSON payload that tells the operator to run `bash scripts/ci-check.sh`. A `?force=1` query-param escape hatch bypasses the gate but emits a `console.warn` audit-log line so emergency rollback streams remain traceable. Combined with D10's existing `scripts/ci-check.sh`, the gate prevents accidentally pushing un-validated code to production VPS. **(D4) Metric hookups + `/metrics` route.** Eight production hookup sites now record real chain telemetry against the existing `metrics::METRICS` registry: `state::apply_block` (height gauge + applied-counter + apply-time histogram numerator/denominator at the success site), `mempool::add` + `mempool::take` (depth + bincode-size bytes gauges, take() also re-computes after drain), `p2p::ConnectionEstablished` + `Closed` (peer-count gauge via `swarm.connected_peers().count()`), `main::try_persist_bft_commit_for` (post-fsync `zvb_bft_commit_persisted_total` inc — placed past every overwrite-skip branch so it fires once per genuinely-new commit blob), `consensus::Producer::run` round-bump branch (`zvb_proposer_round_bumps_total` — operators alert on `rate(...[5m]) > 0` to catch stuck-proposer / liveness-degraded chains; under healthy N=1 this counter stays at 0 forever). The `/metrics` endpoint mounts on the existing RPC port instead of a separate `:9090` listener (rationale comment in `rpc::router`): avoids a new CLI flag / port-binding race, keeps the scrape contract identical for Prometheus, and lets nginx-fronted operators re-route to a dedicated subdomain if isolation is needed. Returns Prometheus text exposition format v0.0.4 with `text/plain` content-type — handler is closure-free over the static `METRICS` singleton. **(D2) Slashing apply path — verifier wired into jail/burn.** New `staking::StakingModule::slash_for_evidence(offender, slot_h, slot_r) → Result<u128>` wraps the existing `slash_double_sign` with a `BTreeSet<(Address, u64, u32)>` dedup field (`#[serde(default)]` for backward-compat with on-disk staking blobs that pre-date this field) — first call burns 5% + jails for `JAIL_EPOCHS_DOUBLE_SIGN` epochs and records the slot; subsequent calls for the SAME `(offender, height, round)` slot return `Ok(0)` no-op. Critical because verified evidence is gossipable: without dedup, a single proof relayed by N peers would slash N times. New `state::State::apply_evidence(ev, current_height) → Result<u128>` is the single chokepoint that turns a verified proof into an on-chain penalty: (1) `evidence::verify_evidence` pure crypto/structural check, (2) replay-window guard rejecting evidence whose slot height is older than `EVIDENCE_REPLAY_WINDOW_HEIGHTS = 17_280` (~24h at 5s blocks — same window Cosmos/Tendermint use, for the same incentive-alignment reason: stale-slashing costs honest re-delegators more than it costs the offender), also rejects future-slot evidence as malicious-or-buggy submitter, (3) dedup-aware `staking.slash_for_evidence`, (4) `zvb_evidence_verified_total` + `zvb_validator_jailed_total` (latter only when burned > 0) metric inc. **What is NOT shipped this batch:** a `TxKind::SubmitEvidence` mempool variant — that would change consensus rules and requires height-gated activation, deferred to the next session. For now `apply_evidence` is reachable from gossip-relayer code paths and unit tests only; the live VPS has nothing calling it yet, so D2 is fully dormant in practice. Seven new unit tests cover happy path, stale-slot rejection, dedup-replay, unknown-offender rejection, plus three `slash_for_evidence` dedup tests in staking.rs (same-slot dedup, distinct-slot still burns, unknown-validator errors). **(F006.3) `FsmAction` → I/O sink (`fsm_runtime.rs`, dormant).** `FsmRuntime` gained a `state: Arc<State>` field + `current_proposal: Mutex<Option<Block>>` cache + new `pub async fn handle_action(action: FsmAction) → Result<Vec<FsmEvent>>` that dispatches every variant: `BuildProposal` calls `producer.build_block()`, caches the result, emits `ProposalSeen`; `CommitBlock` retrieves the cached block, asserts hash match, calls `state.apply_block`, emits `BlockApplied`; `BroadcastPrevote`/`BroadcastPrecommit` are intentional log-only stubs (avoids double-vote risk during F006 cutover — F006.4 will pre-empt the legacy `vote_loop` the same way it pre-empts the legacy producer); `EnteredRound`/`EnteredHeight` log only. The previous `panic!` in `FsmRuntime::run` is replaced with a `tracing::warn!` + 60s sleep loop guard so flipping `ZEBVIX_FSM_ENABLED=true` without F006.4-7 shipped does NOT crash the node — it idles while the legacy `Producer::run` continues to drive consensus byte-identically (strictly safer than crash-on-startup). Three new `tokio::test` cases cover dispatch of all variants against a real `(Producer, State)` fixture. `enabled()` still defaults `false` so live VPS is unaffected. **Dev-environment limitation:** the chain crate cannot be `cargo check`ed in the dev environment because `librocksdb-sys` rebuild exceeds the per-command CPU budget. Operator must run `bash scripts/ci-check.sh` on the build host before pulling the tarball — D10's stale-check now blocks accidental un-validated streams. **Ship-table updates** in `zebvix-chain/HARDENING_TODO.md`: D2 status → 🟢 with footnote on TxKind deferral, D4 status → 🟢 with footnote on RPC-port mount rationale, D10 status → 🟢, F006.3 row added under §B.3.2.7 with footnote describing the warn-and-sleep run() guard.

### Phase D / F006 partial-ship batch (April 2026)
A pragmatic four-item batch landed in `zebvix-chain/` to advance the production-hardening roadmap on items that are **self-contained** (verifiable in isolation), **additive** (zero chain-break risk, VPS untouched, dormant under default env), and **high-ROI** (production-readiness value per LoC). Each item is partial — the cryptographic / data-plane core is shipped today, while the integration hooks that touch RocksDB-using code paths (`state.rs` / `mempool.rs` / `p2p.rs` / `rpc.rs`) are deferred to a follow-up commit when full `cargo check` becomes feasible (current dev env's CPU budget exceeds on `librocksdb-sys` rebuild).

**(1) F006.2 — Vote-pool tally APIs (`zebvix-chain/src/vote.rs`).** Four new read-only methods on `VotePool` extend the existing `power_for` / `has_quorum` / `collect_precommits_for` API surface with the signals the FSM runtime adapter (F006.3) will poll every tick: `tally_prevotes_for(h, r, validator_set) → HashMap<Option<Hash>, u64>` indexes prevote power by target hash in a single slot walk (cheaper than calling `power_for` per candidate); `tally_precommits_for(...)` is the same for precommits (drives the Commit step transition); `max_round_seen(height) → Option<u32>` returns the highest round seen for a height across either vote type (catch-up heuristic input); `power_above_round(h, my_round, vote_type, set) → u64` sums power for unique validators voting at `r > my_round` (the BFT "f+1 votes ahead → jump rounds" signal). Saturating arithmetic prevents overflow on pathological validator sets; the helper dedupes validators (counts at most once across multiple-round votes — matches the f+1-validators-not-events definition). Six new tests cover: tally indexes by target with mixed power weights, tally includes nil-target precommits separately, empty-slot returns empty map, max_round_seen returns highest across out-of-order arrivals (and `None` for untouched heights), power_above_round dedupes a validator voting at multiple rounds and respects vote_type filtering, and removed validators (mid-height) do not contribute power even if their old vote remains in the pool. Pure additive — VotePool API surface grew; nothing else changed.

**(2) D2 Equivocation Evidence module (`zebvix-chain/src/evidence.rs`, ~350 lines incl. tests).** New module shipping the cryptographic verifier for double-sign slashing evidence. Two structs: `EquivocationEvidence { vote_a, vote_b }` (a pair of conflicting `Vote`s by the same validator at the same `(height, round, vote_type)` slot) and an `Evidence` enum with a single `Equivocation` variant (forward-compatible for D3 light-client-attack and proposer-misbehaviour variants later). The `verify_equivocation(ev, chain_id, validators) → Result<()>` function performs six structural + cryptographic checks: same-validator on both votes, same `(height, round, vote_type)` slot, **different** targets (same target = honest duplicate, harmless), matching `chain_id`, both ECDSA signatures verify, offender in active validator set with non-zero voting power. Every check is independent of local node state — given the same `(ev, chain_id, validators)` every honest verifier returns the same Ok/Err, which is the determinism contract slashing requires. Eleven tests cover happy path, nil-vs-block (also slashable), duplicate rejection (same target), different-validators rejection, different-heights rejection, different-rounds rejection, wrong-chain rejection, bad-signature rejection, offender-not-in-set rejection, zero-power rejection, enum dispatch via `verify_evidence`, and serde bincode roundtrip. **Slashing semantics deferred:** the actual `staking::jail(offender, current_height + 50_000)` + 5% stake burn hook lands once D1 multi-validator deploy is live (slashing a single-validator chain is meaningless — there's nobody else to advance the chain).

**(3) D4 Prometheus metrics exporter (`zebvix-chain/src/metrics.rs`, ~310 lines incl. tests).** New module implementing a pure-stdlib Prometheus text-format exporter — no `prometheus` crate dependency (would have pulled in protobuf machinery for an unused gRPC remote-write path). Built on `parking_lot::RwLock<BTreeMap<String, AtomicU64>>` (already in Cargo.toml deps via `once_cell`). `Metrics::new()` pre-registers eleven canonical metrics covering the chain's vital signs: `zvb_block_height` (gauge), `zvb_blocks_applied_total` (counter), `zvb_block_apply_seconds_sum` + `zvb_block_apply_count` (manual histogram numerator/denominator), `zvb_mempool_depth` + `zvb_mempool_bytes` (gauges), `zvb_peer_count` (gauge), `zvb_bft_commit_persisted_total` (counter — B.3.2.5 side-table writes), `zvb_proposer_round_bumps_total` (counter — Tendermint round timeout fires), `zvb_validator_jailed_total` (counter — D2/H5 future), `zvb_evidence_verified_total` (counter — D2 future), `zvb_fsm_step_seconds_sum` (counter — F006.5 future). Public API: `inc(name)`, `add(name, delta)`, `set(name, value)`, `get(name) → u64`, `render() → String` (emits standard `# HELP` / `# TYPE` headers + values, byte-deterministic via BTreeMap ordering for stable scrape hashes). Auto-registers unknown metric names as counters on first touch (forward-compat). A global static `METRICS: Lazy<Metrics>` is exposed so any module can record without threading a handle through the call stack. Eight tests cover counter increment persistence, gauge set-overwrites-not-adds, render-includes-help-and-type, render determinism across repeated calls, auto-registration of unknown metrics, unknown-metric returns zero, all canonical metrics appear in render output at zero, global singleton increment works, and a 10K-iteration high-volume increment smoke test. **Hookups deferred:** the table in the module doc-comment lists every call-site that should `METRICS.inc(...)` — `state::apply_block` end, `mempool::insert` / `take`, `p2p::run_swarm` connection events, `consensus::Producer::run` round-bump branch, `staking::jail`, `evidence::verify_*`, `fsm_runtime` step instants. Wiring lands alongside the `/metrics` axum route in a follow-up commit.

**(4) D10 CI gate script (`zebvix-chain/scripts/ci-check.sh`, ~110 lines bash, executable).** A pre-tarball validation script the operator runs on the build host before pulling a fresh tarball to a production VPS. Three modes: `--quick` runs `cargo check --features zvm` only (sufficient for dev environment where `cargo test` triggers an out-of-budget librocksdb-sys rebuild); full mode (no flag) runs `cargo check` → `cargo clippy --features zvm -- -D warnings -A clippy::all` → `cargo test --lib --features zvm`. Each step is wrapped in a `timeout`-bounded `run` helper with pass/fail reporting and a hint when the hard timeout fires (default 300s, override via `ZVB_CI_TIMEOUT_S`). On full success the script touches `target/.last-ci-pass` so the api-server tarball-handler can (in a follow-up commit) refuse to stream when the marker is older than 24h or when `src/` has been modified since — preventing accidental deploy of un-validated code. Exit codes: 0 = all gates passed and marker touched, 1 = at least one gate failed, 2 = usage / environment error. Argument parsing supports `--help` and rejects unknown flags. Self-locating via `BASH_SOURCE` so it runs from any CWD.

**Module registration.** `zebvix-chain/src/lib.rs` gained `pub mod evidence;` + `pub mod metrics;` lines (sorted alphabetically into the existing module list). No other file in the chain crate was edited — these four items are pure additions. The existing live VPS chain at h≈51260+ continues to produce blocks via the legacy `Producer::run` path with byte-identical behaviour; the new modules compile in but don't execute until F006.3 wires the FSM runtime adapter and a `/metrics` axum route is mounted.

**Ship-table updates** in `zebvix-chain/HARDENING_TODO.md`: F006.2 row added under C1/B.3.2.7 ("✅ shipped dormant"); D2 status changed to "🟡 verifier shipped, jail/burn ⏳"; D4 status changed to "🟡 exporter module shipped, hookups + scrape route ⏳"; D10 status changed to "🟡 `scripts/ci-check.sh` shipped, tarball-handler stale-check ⏳". The 🟡 marker indicates partial-ship — the data-plane / cryptographic core is in, the integration hooks remain.

### Roadmap Tier Index + Tier-3 expansion (C3 / H2 / H5)
A new "Roadmap Tier Index" section near the top of `zebvix-chain/HARDENING_TODO.md` organises all remaining work into three tiers by activation model: **Tier 1 — Consensus correctness** (chain-breaking, height-gated, never flip two on the same restart) covers C1 / Phase B.3.2.x BFT (partially shipped, F006.2-7 pending), C2 Keccak256 signing migration (planned), C3 M-of-N bridge multisig (planned); **Tier 2 — Production maturity** (additive, no chain-break, parallel-shippable) covers all 10 D-items below; **Tier 3 — Performance & polish** (deferred, no urgency) covers H2 Block-STM and H5 gossipsub peer scoring. The Tier-3 entries C3, H2, H5 have been expanded from short paragraphs to Phase-D-level rigor with concrete sub-tasks: **C3.1-7** (BridgeFederation registry struct + RocksDB persistence, admin RPC for federation lifecycle, BridgeInMultisig variant with V1-domain-separator canonical message hash + replay protection via committed src_tx_hash tracking, verification path enforcing threshold + uniqueness + nonce + member-only signers, off-chain oracle coordination spec for docs/BRIDGE_ORACLE.md, height-gated legacy BridgeIn deprecation via ZEBVIX_BRIDGE_MULTISIG_ACTIVATION_HEIGHT env, operator activation runbook with 7-day soak procedure — flagged as highest-priority Tier-1 item after F006 ships because bridge volume grows superlinearly with chain adoption); **H2.1-6** (MVCC version map with VersionedKey/TxReadSet/TxWriteSet structs, Aptos-style Scheduler with rayon worker pool sized to num_cpus, conflict detection + re-execution loop with bumped incarnations, --enable-parallel-exec CLI flag default OFF, determinism test suite over 10K-block recorded mainnet replay as the gating green-light asserting state-root equivalence, criterion benchmark harness with go-criterion ≥2.5x speedup on 4-core VPS — largest individual TODO item at 5 sessions); **H5.1-5** (conservative PeerScoreParams baseline matching Lighthouse/Prysm, peer-score event listener via tokio broadcast channel, peer_id→validator address handshake mapping with signature-over-peer_id replay protection, hook into staking::jail with shorter ~6h jail than D2's ~3-day equivocation jail because gossip misbehaviour is recoverable, 7-day testnet bake-in procedure with false-positive/true-positive/score-distribution tracking — depends on D1+D2+D4 to be meaningful).

### Phase D — Operator Maturity Roadmap (Production-Readiness, planning-only)
A new "Phase D" section at the bottom of `zebvix-chain/HARDENING_TODO.md` catalogues 10 production-readiness items beyond chain correctness — D1 multi-validator decentralisation (N≥3 deploy, depends on F006.7), D2 slashing enforcement (equivocation evidence verifier + jail/burn, depends on D1), D3 state-sync via RocksDB checkpoint snapshot offer/accept (3-session item, unlocks fast new-validator bootstrap once chain grows past ~1M blocks), D4 Prometheus exporter + Grafana dashboard JSON + alert rules (zvb_block_height, zvb_mempool_depth, zvb_peer_count, zvb_fsm_step_seconds, etc), D5 backup & DR (6-hourly RocksDB snapshot pushed via rclone to S3/B2/R2/Hetzner Storage, validator key 2-of-3 Shamir Secret Sharing across geographically separate operators, quarterly DR drill on throwaway VPS, weekly state-integrity self-check) — flagged as the **single largest residual risk** today, D6 block explorer feature parity (sui-fork-dashboard endpoints for BFT commit signers, validator-set, evidence, mempool, plus WebSocket subscription channel), D7 EIP-1559 fee market (base-fee + priority-fee in side-table, height-gated activation pattern same as C2, eth_feeHistory RPC for wallet UX), D8 docs/RUNBOOK.md consolidation (extract scattered procedures into single 8-section runbook shipped in tarball), D9 RPC pagination + range caps (eth_getLogs hard-cap at 10K block range + 5K logs per response with cursor, per-IP rate limit via governor crate, 1MB request body cap), D10 CI gate (scripts/ci-check.sh + tarball-handler refusing to stream when target/.last-ci-pass is stale, ADMIN_TOKEN-gated bypass for emergency rollback). Total estimated effort ~18 sessions. Recommended sequence **D2 → D5 → D4 → D9 → D10 → D1 → D6 → D7 → D3 → D8** prioritises correctness/safety first, decentralisation second, polish last. None are chain-breaking — they can ship in any order, in parallel, by different operators, without coordinating restarts (unlike F006/C2 which need height-gated activation).

### Phase B.3.2.7 — Next Implementation Plan (F006 + C2)
The next consensus-hardening session has two priority-ordered items, both broken into concrete sub-tasks in `zebvix-chain/HARDENING_TODO.md` (see the new "Phase B.3.2.7 — Next Implementation Session Plan" section at the bottom of that file). **F006 (priority #1)** wires the shipped FSM module into `Producer::run` behind the `ZEBVIX_FSM_ENABLED` env flag (default OFF). It is split into 7 sub-tasks: F006.1 adapter scaffold (`src/fsm_runtime.rs`), F006.2 vote-pool → `FsmEvent` translator with quorum-edge dedup, F006.3 `FsmAction` → I/O sink with apply-ack feedback loop, F006.4 startup recovery for in-flight commits via side-table inspection, F006.5 observability (structured logs for stuck-in-Commit, dropped wrong-height/wrong-hash events, round-bump counts), F006.6 N=1 cadence preservation (rate-limit proposals to honour 5s `BLOCK_TIME_SECS`), F006.7 test matrix + VPS deploy-ordering gate ("ENABLED=false produces byte-identical blocks to legacy" is the gating green-light). **C2 (priority #2)** is the SHA-256 → Keccak256 signing migration for EVM-native MetaMask compat, broken into 6 sub-tasks: C2.1 dual signing API in `crypto.rs` (legacy SHA path stays byte-stable), C2.2 height-gated dispatch in `state::apply_block`, C2.3 mempool propagation, C2.4 RPC submission paths (`eth_sendRawTransaction` + `zvb_submitTx`), C2.5 CLI + Flutter wallet tooling, C2.6 operator activation run-book mirroring the BFT-commit-gate pattern. C2 cannot brick the chain because the activation env defaults to `u64::MAX`, but it requires careful operator coordination (chain binary → activation env → wallet config → user-visible flip). Cross-dependencies: none at the code level — different modules. Deploy ordering: F006-dev, C2-dev, F006-mainnet (≥ 7 days soak), C2-mainnet — never two height-gates flipping on the same restart.

### Dashboard Production Hardening
The dashboard (`artifacts/sui-fork-dashboard`) ships an opt-in **wallet vault** (`src/lib/wallet-vault.ts`) using AES-GCM 256 with PBKDF2-SHA-256 key derivation (200k iterations). When the vault is unlocked, the derived `CryptoKey` is held in module memory so subsequent wallet writes auto-re-encrypt the on-disk blob without re-prompting; sessionStorage acts as a per-tab plaintext mirror that survives reloads but evicts on tab close. UI lives in `src/components/wallet/VaultControls.tsx`. The chain-source explorer (`/api/chain/file`, `/api/chain/tree`) is gated behind `CHAIN_EXPLORER_PUBLIC=1` (default OFF in production) with async fs, depth caps, and path-traversal guards. The `/api/rpc` proxy no longer leaks `VPS_RPC_URL` in error responses and rejects oversized methods/non-array params. The frontend wraps routes in an `ErrorBoundary` keyed on location, configures React Query with `staleTime: 30s` + `refetchOnWindowFocus: false`, drops `console`/`debugger` from production builds via `esbuild.drop`, and uses a `usePagePolling` hook so chain pollers pause when the tab is hidden.

### Wallet Page Production Polish (April 2026)
The MetaMask tab and the entire `MetaMaskTab` component were removed from `/wallet` (the `src/lib/metamask.ts` helper has no remaining importers). The page now exposes four tabs — **Send / Receive / Manage / History**. The new **Receive** tab renders a 208px QR (`qrcode.react` `QRCodeSVG`) that encodes either the bare address or, when a positive amount is entered, an EIP-681 payment URI of the form `ethereum:<addr>@7878?value=<wei>`. The active-wallet card now shows a live USD valuation derived from `zbx_getPriceUSD` (polled every 15s and paused when the tab is hidden), a network heartbeat pill (`zbx_blockNumber` tip with a pulsing dot), and inline **Receive** + **Explorer** action buttons. The page subtitle was rewritten to remove the legacy "MetaMask flow for Solidity tx" copy.

## Censorship-Resistance Guarantees
Zebvix prevents administrative interference with user transfers for core transaction types, limiting control to `ValidatorAdd/Edit/Remove`, `GovernorChange`, and specific `Bridge` operations. The mempool does not have an admin filter or address blacklist.

### Phase B.3.2.7 — F006.4 ship: FSM event sources + dispatch loop (April 28, 2026)
A fifth `zebvix-chain` ship batch advances the FSM runtime adapter from F006.3's warn+sleep guard to a fully wired event-driven loop, while staying dormant on the live VPS chain (h≈54600+ at deploy, 5s pacing intact). The single touched file is `zebvix-chain/src/fsm_runtime.rs`. **(a) Vote-pool watcher (`poll_vote_quorums`).** Walks `tally_prevotes_for` / `tally_precommits_for` (F006.2 APIs) at the FSM's current `(height, round)` and emits one `FsmEvent::PrevoteQuorum` / `PrecommitQuorum` per target whose voting power crosses the 2/3+ threshold. Dedup via a `Mutex<HashSet<(u64, u32, VoteType, Option<Hash>)>>` so a stable quorum doesn't re-fire on every poll; the set is cleared on `EnteredHeight` action so the new height observes its own quorums fresh. Lock discipline is sequential — `fsm` mutex acquired (read-only snapshot of height/round) and released, then `last_emitted_quorum` mutex acquired and released — never holds two mutexes simultaneously, eliminating any deadlock risk against `tick_once`. **(b) Tick clock (`tick_once`).** `pub async fn tick_once(&self, now: Instant) -> Result<Vec<FsmEvent>>` feeds a single `FsmEvent::Tick(now)` into the FSM and dispatches every emitted action through `handle_action`. Returns the downstream events those dispatches produced (e.g. `CommitBlock` returns `BlockApplied`) so the caller can re-feed them on the next iteration. The loop's tick cadence is `tokio::time::interval(Duration::from_millis(500))` matching legacy `consensus::Producer::run` exactly — the existing F006.1 `parity_with_legacy_propose_timeout` test (compile-time tied to `consensus::PROPOSE_TIMEOUT_SECS`) keeps the round-bump cadence in lockstep with legacy. **(c) Bounded proposal cache (`proposal_cache: Mutex<BTreeMap<u64, Block>>`).** Capped at `PROPOSAL_CACHE_CAP = 64` entries (~5min of N=1 operation, ~100MB at 1.5MB/block, well below RSS budget). `pub async fn cache_proposal(&self, block: Block)` inserts at `block.header.height`; on cap overflow, the smallest height is popped first (BTreeMap iteration order is key-ordered). `handle_action(CommitBlock)` sources the actual `Block` bytes from this cache for `state.apply_block`. No production caller exists yet for `cache_proposal` — a future p2p ingress hook (F006.4.5, can bundle with F006.7) will populate it. **(d) Recovery helper (`recover_from_state(state) -> u64`).** Returns `state.tip().0 + 1`. Honors the existing `State::open` partial-write recovery guard (`state.rs` lines 2767-2780): if the last commit fsync was torn, `State::open` already errors at startup and the operator must clear the recovery marker before re-attempting — by the time `recover_from_state` runs, `state.tip()` is guaranteed to be a fully-committed height. `FsmRuntime::new` calls this internally; the old `start_height: u64` constructor arg is gone (cleaner, less footgun). **(e) Real event loop (`run`).** Replaces F006.3's warn+sleep with a tick-driven sequential loop: `tokio::time::interval(500ms)` wake-up → `tick_once` → `poll_vote_quorums` → drain a pending-event queue through `step` until exhausted, folding every `handle_action` return value (e.g. `BlockApplied` from a `CommitBlock` dispatch) back into the queue inside the same tick. This implements a closed-loop "event → action → event" pipeline so the FSM advances out of `Step::Commit` immediately rather than waiting for the next 500ms wake-up. Hard cap `MAX_EVENTS_PER_TICK = 64` as a defensive belt against feedback runaway (FSM monotonicity already bounds the queue, but the cap is loud-fail explicit). Not yet a `tokio::select!` because the only awaitable source today is the tick interval — the watcher is a synchronous poll, not a channel — so `select!` would degenerate to a single-arm `select!` and add no value. F006.4.5 (proposal gossip ingress, can bundle with F006.7) will add a real second source — a channel of incoming `cache_proposal` calls from p2p — at which point the loop becomes a proper `tokio::select!` over both arms; keeping the shape simple now reduces F006.7 review surface. Disabled-fast path: when `enabled()` returns false (default), `run()` returns immediately — no warning, no busy-wait, no infinite loop — so a future `main.rs` change to unconditionally `tokio::spawn(rt.run())` on every startup is safe. **Shadow-observer mode** (default even when `enabled()=true`): `BuildProposal` / `BroadcastPrevote` / `BroadcastPrecommit` actions remain log-only stubs (matches F006.3 behaviour). Only `CommitBlock` reaches `state.apply_block`, and it is gated by a `state.tip() < height` safety check that prevents double-apply over a height the legacy path already finalised. The legacy `Producer::run` is intentionally NOT pre-empted in F006.4 — that's F006.7's job, after F006.5 (cadence) + F006.6 (gating soak) prove the FSM byte-identical to legacy. Pre-empting in F006.4 without the cadence rate-limiter and proposal gossip ingress wired would risk forking under a cold-start race. Shadow-observer mode lets the FSM code paths be soaked in production (with `enabled()=true` set out-of-band on a test VPS) without touching the legacy consensus drive, so any regression discovered late is recoverable by simply unsetting the env var. **Five new tests (chain test count 192 → 198 on VPS CI 2026-04-28; one drifts above the planning estimate of 197 — likely a doctest or an existing test count nit, all green):** `poll_vote_quorums_emits_prevote_quorum_under_n1` (T001 acceptance — 1/1 vote crosses quorum, dedup suppresses repeat emission), `tick_once_without_quorum_is_noop_on_height` (T002 — tick alone doesn't advance height), `proposal_cache_caps_and_evicts_smallest_height` (T003 — insert 70 blocks, assert cap=64 + smallest evicted), `recover_from_state_returns_tip_plus_one` (T004 — fresh state tip 0 → returns 1), `run_returns_immediately_when_disabled` (T005 — `run()` exits in <100ms when env var unset). Plus three `handle_action_*` test updates for the new shadow-stub semantics: `BuildProposal` is now a no-op (no `current_proposal` field, no `producer.build_block()` call); `CommitBlock` below tip is idempotent (returns `BlockApplied` ack without applying — legacy already did); `CommitBlock` above tip without a cached proposal returns a clear "no proposal cached" error so the F006.4.5 wiring requirement is loud at runtime. **Ship-table updates** in `zebvix-chain/HARDENING_TODO.md`: F006.4 row added under §B.3.2.7 (`✅ shipped dormant ⁴`), F006.5 → F006.7 collapsed into one remaining `⏳ planned` row, new footnote ⁴ describing the shadow-observer design + the explicit F006.7 deferral rationale + the test count, F006.4 detail block at line 961+ rewritten from "planned" to "✅ SHIPPED" with sub-bullets for each component. **Dev-environment limitation** (unchanged from F006.3): the chain crate cannot be `cargo check`ed in the dev environment because `librocksdb-sys` rebuild exceeds the per-command CPU budget. Operator runs `bash scripts/ci-check.sh` on the build host before pulling the tarball; D10's stale-check (24h `target/.last-ci-pass` marker, audit-logged `?force=1` escape) blocks accidental un-validated streams. **Live VPS impact:** none. Rebuild + binary swap + `systemctl restart zebvix` should show `mode=ROUND_ROBIN+TIMEOUTS` in the journal (legacy producer still in charge), `zvb_block_height` advancing at 5s cadence, FSM dormant.

### Phase B.3.2.7 — F006.4 LIVE deploy verification + VPS port surface inventory (April 28, 2026)
After the F006.4 ship-batch documentation above, the binary actually went live on VPS srv1266996 at **2026-04-28 00:55:07 UTC**. This section captures the deploy outcome and clears up an operator-runbook trap (port 10000) that bit the post-deploy monitoring step. **Build + swap.** Operator pulled the source tarball (354 KB) from the dev workspace api-server's `/api/download/newchain` route into `/home/zebvix-chain`, ran `bash scripts/ci-check.sh` (D10 gate — `cargo check` + `cargo clippy --features zvm` denying warnings + `cargo test --lib --features zvm`) which reported `test result: ok. 198 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.85s` plus updated the `target/.last-ci-pass` marker for the 24 h tarball-handler stale-check, then `cargo build --release --features zvm` produced a 16 MB binary in 1m 35s. Pre-deploy backup `cp /usr/local/bin/zebvix-node /usr/local/bin/zebvix-node.last-good`, then `systemctl stop zebvix && cp target/release/zebvix-node /usr/local/bin/ && systemctl start zebvix` — service active again within ~3 seconds. **Liveness verification.** Pre-deploy tip h=54897 (from the producer startup banner). Post-deploy first observed quorum log h=54928 at 00:58:08 UTC (3 minutes after start, including the consensus warm-up). Cadence sanity check via `eth_blockNumber` JSON-RPC over the next 24 seconds: h=54976 → 54977 → 54978 → 54979 → 54980, exactly +1 per ~6 s iteration matching the producer banner's `pacing=5s propose_timeout=8s tick=500ms`. Journal grep confirmed legacy producer in charge: `mode=ROUND_ROBIN+TIMEOUTS (B.3.2.2) pacing=5s propose_timeout=8s tick=500ms` at boot, then standard `🗳 emitted prevote h=N block=0x... ✅ QUORUM` / `🗳 emitted precommit h=N block=0x... ✅ QUORUM` lines in the consensus.rs format (NOT the new fsm_runtime.rs format) — proving the FSM is dormant exactly as designed. Zero `fsm_runtime` log lines in the journal because `main.rs` does not yet construct `FsmRuntime`; the new code soaks purely as a static-analysis target until F006.7 wires the spawn. **Rollback ready.** `/usr/local/bin/zebvix-node.last-good` saved on VPS — single-command revert via `systemctl stop zebvix && cp /usr/local/bin/zebvix-node.last-good /usr/local/bin/zebvix-node && systemctl start zebvix`. **VPS port surface inventory** (this is the part future operators / agents must internalise to avoid the post-deploy monitoring trap): the live VPS exposes **three** TCP listeners owned by zebvix-node — `:30333` libp2p p2p networking (Substrate-style default), `:8545` JSON-RPC EVM-compat namespace (`eth_*` methods including `eth_blockNumber` for height monitoring), and the `/metrics` Prometheus endpoint **mounted on `:8545` itself** per D4 footnote ³, NOT a separate `:9090` listener. There is also a `:10000` HTTP listener on the box but it belongs to **Webmin** (`Server: MiniServ` in the response headers, returns 302 redirect to the admin panel) — **NOT zebvix**. The pre-flight monitoring snippet in the agent's deploy script mistakenly probed `http://localhost:10000/metrics` for the `zvb_block_height` gauge and got empty output because the request landed on Webmin's admin UI instead of the chain's metrics endpoint. The chain itself was always healthy; the empty curl response was a false-negative monitoring trap. **Correct liveness probe** (use this in all future ship verifications): `curl -s -X POST -H "Content-Type: application/json" --data '{"jsonrpc":"2.0","method":"eth_blockNumber","params":[],"id":1}' http://localhost:8545` returns `{"jsonrpc":"2.0","id":1,"result":"0x<hex-height>"}` — convert with `printf '%d\n' 0x<hex>` to get decimal. **Custom `zvb_*` JSON-RPC namespace and Substrate-style `rpc_methods` introspection are NOT exposed** on this build; only the Ethereum-standard `eth_*` namespace is reachable on `:8545`. Adding a `zvb_chainTip` / `zvb_validators` / `zvb_bftCommit` namespace is queued in the deferred Observability batch alongside future Grafana dashboard hookups. **Followup queue (priority order):** (1) 🔴 ADMIN_TOKEN rotation — outstanding security debt from a prior session leak, must rotate before any further admin-RPC work; (2) 🟡 F006.5 cadence rate-limiter (FSM locked to legacy 5s pacing for byte-identical commit blobs); (3) 🟡 F006.6 gating soak harness (1000-block FSM-shadow vs legacy commit-blob byte-diff); (4) 🟢 F006.7 legacy `Producer::run` pre-emption + `ZEBVIX_FSM_ENABLED=true` activation (the actual handoff); (5) 🟢 F006.4.5 proposal gossip ingress (p2p → `cache_proposal` channel) — can bundle with F006.7; (6) 🟢 Observability batch — `/metrics` Prometheus surface verification + `zvb_*` JSON-RPC namespace addition + Grafana dashboards. F006.5 → F006.6 → F006.7 are sequential dependencies — each gates the next, ek session mein ek hi shipp karna recommended kyunki har ek production-touching hai.

# External Dependencies

- **Monorepo Management:** pnpm workspaces
- **Blockchain Core:** Rust, RocksDB, `k256`
- **Web Server:** Express 5
- **Database:** PostgreSQL, Drizzle ORM
- **Validation:** Zod
- **API Codegen:** Orval
- **Build Tool:** esbuild
- **Cryptography (JS):** `@noble/hashes/sha3.js`, `@noble/curves/secp256k1`
- **Mobile Development:** Flutter, `flutter_secure_storage`, `web3dart`
- **Smart Contracts:** Solidity 0.8.24, OpenZeppelin
- **Client Libraries (Dashboard):** ethers.js