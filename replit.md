# Overview

This project is a pnpm workspace monorepo utilizing TypeScript to build the Zebvix L1 blockchain. The core purpose is to create a performant, EVM-compatible blockchain with integrated DeFi capabilities (zSwap AMM), robust governance, and seamless cross-chain bridging. The project aims to establish Zebvix as a key player in the decentralized ecosystem by offering a secure, scalable, and user-friendly platform.

Key features include:
- A custom L1 blockchain (`zebvix-chain`) implemented in Rust with token ZBX (chain-id 7878), EVM-style addresses, and Bitcoin-like halving.
- A permissionless zSwap Automated Market Maker (AMM) integrated directly on-chain.
- A comprehensive dashboard for monitoring, interaction, and development.
- Mobile wallet connectivity with QR pairing.
- EVM execution layer (Cancun fork) for smart contract deployment and interaction.
- Cross-chain bridge module for interoperability with external networks like BNB Chain.

The project is structured to ensure high performance, security, and extensibility, leveraging modern tooling and architectural patterns.

# User Preferences

Communicate in Hinglish with the user.

# System Architecture

## Monorepo Structure
The project is organized as a pnpm workspace monorepo, with each package managing its own dependencies. Key packages include:
- `zebvix-chain/`: The core Rust implementation of the Zebvix L1 blockchain.
- `api-server/`: Express 5 based API server.
- Dashboard (`artifacts/sui-fork-dashboard/`): React-based frontend for chain interaction and monitoring.
- `mobile/zebvix-wallet/`: Flutter-based mobile wallet application.

## Core Technologies
- **Backend:** Node.js 24, TypeScript 5.9, Express 5, Rust for blockchain core.
- **Database:** PostgreSQL with Drizzle ORM for off-chain data, RocksDB for on-chain state.
- **Validation:** Zod (`zod/v4`), `drizzle-zod`.
- **API Codegen:** Orval (from OpenAPI spec).
- **Build System:** esbuild (CJS bundle).

## Zebvix L1 Blockchain (`zebvix-chain/`)
- **Consensus:** 2-node sync (P2P gossip, heartbeat, block sync), RocksDB-backed validator registry, Ed25519 votes with double-sign detection, 2/2 quorum for block verification, round-robin proposer.
- **Transactions:** `TxKind` enum supports `Transfer`, `ValidatorAdd`, `ValidatorRemove`, `Swap`, `Bridge`, and `Proposal` (Phase D governance).
- **Tokenomics:** 150M ZBX supply, Bitcoin-style halving.
- **AMM:** On-chain zSwap AMM with initial seed at $0.50/ZBX. Supports `SwapDirection` (ZbxToZusd, ZusdToZbx) with slippage protection.
- **Recent Transactions:** On-chain `RecentTxRecord` index (RocksDB-backed ring buffer) for fast retrieval of past transactions without scanning blocks.
- **Cryptography:** Switched from Ed25519 to secp256k1 (ECDSA) for ETH-compatible address derivation (`keccak256(uncompressed_pubkey[1..])[12..]`).
- **Bridge Module:** On-chain `bridge` module with `BridgeNetwork` and `BridgeAsset` registries, lock/release pattern for cross-chain transfers. Admin-extensible for new networks and assets.
- **Phase D — Forkless On-chain Governance:** `proposal` module with `ProposalKind` (FeatureFlag, ParamChange, ContractWhitelist, TextOnly). Wallets holding ≥ 1 000 ZBX may submit proposals (only fee consumed; principal refunded). Lifecycle: 14-day shadow-execution Testing phase → 76-day Voting phase (90 days total). 1 wallet = 1 vote (no balance weighting; voters only pay gas). Auto-activates iff yes/total ≥ 90% AND total ≥ 5; activation flips a feature flag, sets a u128 param, or whitelists a contract — no hard fork. Max 3 active (Testing|Voting) proposals per proposer. State persisted in `CF_META` under `prop/`, `prop_vote/`, `prop_active/`, `prop_count`, `ff/`, `ff_label/`. RPC: `zbx_proposalsList`, `zbx_proposalGet`, `zbx_proposerCheck`, `zbx_proposalHasVoted`, `zbx_proposalShadowExec` (strictly read-only, never mutates consensus state), `zbx_featureFlagsList`, `zbx_featureFlagGet`. CLI: `propose`, `vote`, `proposals-list`, `proposal-get`, `feature-flags-list`. Dashboard `/governance` page with eligibility check, proposals list, feature-flag sidebar, and shadow-exec preview. Status labels are capitalized end-to-end (`Testing`, `Voting`, `Approved`, `Rejected`, `Activated`).

## EVM Integration
- **EVM Execution Layer:** Native EVM implementation (Cancun fork) in Rust, gated behind `cargo --features evm`.
- **Opcode Support:** Full Cancun opcode set, including CREATE/CREATE2.
- **Gas Accounting:** Mainnet-matching gas constants, quadratic memory expansion, SSTORE refunds.
- **Storage:** `CfEvmDb` with in-memory account cache, atomic journal applies.
- **Precompiles:** Standard (ECRECOVER, SHA256, IDENTITY) and custom (bridge_out, payid_resolve, amm_swap, multisig_propose).
- **JSON-RPC:** Comprehensive `eth_*` namespace (15 methods) for EVM interaction.
- **Solidity Contracts:** Drafted Solidity 0.8.24 contracts (`ZBX20.sol`, `BridgeVault.sol`, `BridgeMultisig.sol`, `Multicall3.sol`, `ZbxStaking.sol`, `ZbxAMM.sol`, `ZbxTimelock.sol`) for the BNB-Chain side of the bridge and general utility.

## User Interface (Dashboard)
- **Mission Control:** Rewritten home page showing live block height, chain stats, KPIs, recent blocks, quick access grid, chain identity, and MetaMask connection.
- **EVM Explorer:** New page for interacting with the native EVM, including a unified Smart Search bar (auto-detects EVM address, 32-byte hash, block number/tag, and Pay-ID alias and routes to the right RPC, with cross-link buttons between results and a dual-source block fetch — `zbx_getBlockByNumber` for numeric heights and `eth_getBlockByNumber` for tags), net status, balance/transaction/code/block lookup tools, and a raw JSON-RPC dispatcher.
- **Pool Explorer:** Page dedicated to monitoring the zSwap AMM pool, displaying reserves, k-invariant, loan repayment progress, quote calculator, and recent swaps.
- **Tokenomics:** Live RPC pull of `zbx_supply` every 10s; distribution bar (6.66% founder premine + 13.33% foundation + mined + burned + remaining); Foundation Treasury & AMM Pool Seed cards; block reward + gas split + burn + Phase A/B halving mechanics. Points to `zebvix-chain/src/tokenomics.rs`.
- **Smart Contracts (EVM):** New page at `src/pages/smart-contracts.tsx` (route `/smart-contracts`, sidebar entry "Smart Contracts (EVM)"). 13-round architect-verified, line-by-line accurate against zebvix-chain Rust source. Replaces Sui-template's Move-language page. Hero (Cancun-targeted EVM, useful subset, gated by `--features evm`) + 4 stat tiles (chain_id 7878, ZBX 18-dec gas token, secp256k1 senders, JSON-RPC 8545). What works today: Solidity 0.8.24 → bytecode → `EvmBytecode::deploy` via `eth_sendRawTransaction` (CREATE/CREATE2/CALL/STATICCALL via 4 frame types, full memory/storage/journal/revert semantics, MCOPY EIP-5656, BLOBHASH stub returns 0, SELFDESTRUCT rejected). Strict caveats listed: (a) signed-arithmetic `SDIV`/`SMOD`/`SLT`/`SGT`/`SAR` + `EXTCODECOPY`/`RETURNDATACOPY` not yet dispatched; MULMOD IS at 0x09; (b) precompiles 0x01/0x02/0x04 full; 0x03 RIPEMD160 gas-correct zero-output stub; 0x05 MODEXP fixed-200-gas placeholder (no EIP-2565 dynamic pricing); 0x06–0x09 (alt_bn128, blake2f) deferred; (c) custom Zebvix precompiles 0x80–0x83 (bridge/AMM/multisig/Pay-ID) return preview values for gas/ABI shape but native side-effects NOT committed on `eth_sendRawTransaction` path — production must use `zbx_*` RPC namespace; post-frame intent capture is remaining C.2 work; (d) EIP-2929/3529 warm/cold split not modelled — single-tier costs `G_SLOAD=2100`, `G_BALANCE`/`G_EXTCODE`/`G_CALL=2600`; (e) refund cap `gas_used/5` is target — interpreter only ACCUMULATES SSTORE clear refunds today, cap enforcement deferred to C.3 gas-settlement path; (f) `DEFAULT_BLOCK_GAS_LIMIT=30M` compiled-in, read every tx in `EvmRpcCtx::evm_context()`; ParamChange API live in Phase D but runtime read deferred to C.3; (g) `eth_sendRawTransaction` accepts EIP-1559 (type-2), EIP-2930 (type-1), AND EIP-155-protected legacy envelopes; rejects unprotected legacy; (h) `eth_getBalance` routed by legacy `rpc.rs` against `CF_ACCOUNTS` — EVM-side balance changes are journaled into `CF_EVM` via `apply_journal` and NOT synced back, so two ledgers can diverge for same secp256k1 addr after EVM activity (cross-domain settlement = C.3); `eth_getTransactionCount`/`getCode`/`getStorageAt` via `evm_rpc::dispatch` over `CF_EVM`; (i) **logs/receipts gap:** `store_logs` is defined in `evm_state.rs` but has ZERO call sites — neither EVM tx path nor native modules write to `CF_LOGS` today, so `eth_getLogs` always returns `[]` and `eth_getTransactionReceipt` returns `null`; emitted log entries carry `tx_hash=0x00`. C.3 wires producers + canonical tx-hash stamping + receipts table. Dashboard's Hardhat snippet recommends re-reading state via `balanceOf` rather than getLogs/getTransactionReceipt for verification. CF_LOGS storage section, JSON-RPC routing table, opcode/precompile lists, and architecture diagram all reflect this reality.
- **Cross-Chain Bridge (Phase B.12):** Page at `src/pages/bridge.tsx` (route `/bridge`). 4-round architect-verified, line-by-line accurate against `zebvix-chain/src/bridge.rs` + `state.rs::apply_tx` + `rpc.rs` + `main.rs` CLI + `tokenomics.rs`. Replaces Sui-template's fictional Move-module + user-deployed-Solidity bridge page. Hero (Phase B.12 LIVE / single-trusted-oracle MVP / lock-and-mint + burn-and-release) + 4 stat tiles (direction ZBX/zUSD ↔ EVM, lock vault `0x7a627264670…` = ASCII "zbrdg", admin-registered networks, native ZBX 18-dec / zUSD 6-dec). Trust callout (yellow) lists 5 explicit limitations: (a) admin = oracle = single drain vector; (b) no on-chain proof of foreign deposit, only replay protection via 32-byte hash marker; (c) `MAX_OUT_EVENTS=4096` ring buffer with eviction warning; (d) wrapped-token decimals stored as hint, not scaled — both sides must agree off-chain; (e) no fee market on bridge tx. Architecture ASCII diagram showing 2-leg flow (user `BridgeOut` → `state.rs::apply_tx` → BridgeOutEvent ring → off-chain oracle polls `zbx_recentBridgeOutEvents` → mints wrapped on foreign chain; reverse: foreign vault deposit → oracle reads → submits `BridgeIn { source_tx_hash }` → chain releases native + marks claim used). All 6 `BridgeOp` variants tabled with admin/user gating + state.rs effect (RegisterNetwork, SetNetworkActive — corrected to clarify it only blocks BridgeOut, NOT BridgeIn which checks asset-level active flag only; RegisterAsset, SetAssetActive, BridgeOut, BridgeIn). CF_META storage layout table (b/n/, b/a/, b/c/, b/e/, b/m/seq, b/m/lz, b/m/lu, b/m/aid/) + asset_id encoding `(network_id << 32) | local_seq`. All 7 `zbx_*` bridge RPCs tabled with params/returns. Three CLI workflow code blocks using actual clap kebab-case flat subcommands (`bridge-register-network`, `bridge-register-asset`, `bridge-out`, `bridge-in`, `bridge-networks`, `bridge-assets`, `bridge-stats`) — bootstrap registry, user bridge-out, admin bridge-in with replay-rejection example. Off-chain oracle pseudocode (operator-supplied — no reference oracle in repo) covering both legs with rescale logic and `zbx_isBridgeClaimUsed` idempotency check. Future hardening list (multisig oracle, SPV proof, fee market, throttle, EVM precompile 0x80 wiring tied to C.2 finishing work). EVM contract/dest validation correctly documented as "40 hex chars, optional 0x prefix" (validators do `strip_prefix("0x").unwrap_or(...)`).
- **Implementation Roadmap (Phase Log):** Page at `src/pages/implementation.tsx` (route `/implementation`, sidebar "Implementation Roadmap"). 3-round architect-PASS, line-by-line accurate against the actual `//! Phase X.Y` markers in `zebvix-chain/src/`. Replaces the previous Sui-fork fiction (which referenced `crates/sui-types/`, `MIST_PER_ZBX`, Move smart contracts, BLS12381 protocol keys, `validator.yaml`, `sui-cli`, 0.4s blocks, mist 10⁹ units — none of which exist on Zebvix). Hero clarifies this is NOT a setup tutorial (those go in Environment Setup + Launch Checklist) but a "what shipped, when, what is pending" view, useful for deciding whether to build on a feature today vs wait. Includes a compact phase strip (color-coded LIVE / PARTIAL / PLANNED status badges with icon glyphs), then expandable accordion sections for each phase. Coverage: **Phase A LIVE** (p2p.rs — libp2p 0.54, gossipsub strict + 4 topics on `zebvix/7878/{blocks,txs,heartbeat,votes}/v1`, mDNS, request_response sync `/zebvix/sync/1.0.0` with `SYNC_BATCH_MAX=256`); **B.1 LIVE** (types.rs — Validator { pubkey: 33-byte secp256k1, voting_power: u64 } persisted, mutated by ValidatorAdd/Remove/Edit); **B.2 LIVE** (vote.rs — Tendermint Prevote/Precommit, anti-double-sign keyed by `(height, round, vote_type)` + per-validator slot, returns `AddVoteResult::DoubleSign { previous }` for slashing evidence; not yet quorum-gating); **B.3.1 LIVE** (transaction.rs — TxKind enum, source-exact variant list: Transfer, ValidatorAdd, ValidatorRemove, ValidatorEdit, GovernorChange, Staking(StakeOp), RegisterPayId{pay_id,name}, Multisig(MultisigOp), Swap{direction,min_out}, Bridge(BridgeOp), Proposal(ProposalOp)); **B.3.2 LIVE** (consensus.rs — round bumping; ValidatorEdit + GovernorChange with MAX_GOVERNOR_CHANGES cap); **B.4 LIVE** (staking.rs — share-based delegated, MIN_SELF_BOND_WEI=100 ZBX, MIN_DELEGATION_WEI=10 ZBX, EPOCH_BLOCKS=17280, UNBONDING_EPOCHS=7, slash 5%/0.10% primitives, two-tier CreateValidator + ValidatorAdd seating); **B.7 LIVE** (RegisterPayId + zbx_lookupPayId/zbx_getPayIdOf/zbx_payIdCount RPCs, format `<handle>@zbx`, permanent); **B.8 LIVE** (multisig.rs — 5 ops Create/Propose/Approve/Revoke/Execute, MIN_OWNERS=2 MAX_OWNERS=10, default expiry 17280 blocks, max 1M blocks, v1 only models MultisigAction::Transfer); **B.10 LIVE** (pool.rs — single ZBX/zUSD x·y=k, 0.3% fee bucket, 10M zUSD genesis loan repayment then 50/50 admin/LP split, TxKind::Swap{direction:SwapDirection::{ZbxToZusd|ZusdToZbx}, min_out:u128}); **B.11 LIVE** (crypto.rs — k256 ECDSA, MetaMask keys work directly, founder addr `0x40907000ac0a1a73e4cd89889b4d7ee8980c0315` deterministic from `keccak256("zebvix-genesis-founder-v1")`; SignedTx{body, pubkey:[u8;33], signature:[u8;64]} with NO recovery-id byte — sender RECOMPUTED from pubkey, not recovered); **B.12 LIVE** (bridge.rs — see Cross-Chain Bridge page); **C.1 LIVE** (envelope inspection only); **C.2 PARTIAL** (gated `--features evm`, all caveats from Smart Contracts page distilled including custom precompile mapping correctly listed as 0x80 PC_BRIDGE_OUT / 0x81 PC_PAYID_RESOLVE / 0x82 PC_AMM_SWAP / 0x83 PC_MULTISIG_PROPOSE; eth_sendRawTransaction full RLP+ECDSA recover vs eth_call uses parse_call_envelope JSON-only no-signature simulation path); **D LIVE** (proposal.rs — 90d lifecycle, ≥1000 ZBX threshold, ≥90% yes + ≥5 quorum, FeatureFlag/ParamChange/ContractWhitelist/TextOnly, max 3 active per proposer); **C.3 PLANNED** (cross-domain CF_EVM↔CF_ACCOUNTS settlement, EIP-2929/3529 warm/cold gas + refund-cap enforcement, CF_LOGS producers + canonical tx-hash + receipts table, ParamChange runtime read for DEFAULT_BLOCK_GAS_LIMIT, custom-precompile post-frame intent capture re-issuing native dispatches BridgeOp::BridgeOut/RegisterPayId/Swap/MultisigOp::Propose, signed-arith SDIV/SMOD/SLT/SGT/SAR + EXTCODECOPY/RETURNDATACOPY + MODEXP EIP-2565 + alt_bn128/blake2f); **Future PLANNED** (bridge multisig oracle, bridge SPV, slashing auto-enforcement, libp2p `with_existing_identity` peer-id pinning, block_stm.rs parallel execution, bridge fee market via ParamChange). Round-1 architect caught 6 drifts (Sui-leftover names: ValidatorSetVotingPower→ValidatorEdit, GovernorRotate→GovernorChange, PayIdRegister→RegisterPayId, zbx_resolvePayId→zbx_lookupPayId/zbx_getPayIdOf, multisig 4-op→5-op with Revoke + v1 Transfer-only, custom precompile slot mismatch 0x81/0x82/0x83 swapped, signature wire claimed +1-byte recovery id which doesn't exist, eth_call falsely shared sender-recovery code path with eth_sendRawTransaction, anti-double-sign missing vote_type) all fixed in Round 2; Round 2 caught residual ValidatorSetVotingPower in B.1 detail, fixed in Round 3 → PASS.
- **Network Configuration:** P2P-layer documentation derived from `zebvix-chain/src/p2p.rs`. Hero (libp2p 0.54 / TCP+Noise+Yamux / 4 gossipsub topics / sync batch 256) + 4 stat tiles. Network-stack table (Transport: tcp::Config nodelay; Encryption: noise XX; Multiplexer: yamux; Identity: ed25519 from `SwarmBuilder::with_new_identity()` — independent of secp256k1 chain key, peer-id rotates on EVERY node start; Pub-sub: gossipsub strict mode, 2s heartbeat, 1MiB max, DefaultHasher dedupe; Discovery: mDNS via `--no-mdns`; Sync: request_response::cbor `/zebvix/sync/1.0.0` 15s timeout; Idle: 60s). Gossipsub topics table (4 chain-id-namespaced topics: `zebvix/7878/blocks/v1` bincode(Block) every 5s, `txs/v1` bincode(SignedTx) on submit, `heartbeat/v1` bincode({tip:u64}) every 8s, `votes/v1` bincode(Vote) **Phase B.2 LIVE** — every registered validator auto-signs Prevote+Precommit per new tip, gossiped into shared VotePool, observable via `zbx_voteStats`). Block-sync section with SyncReq/SyncResp Rust shapes + 2 triggers (out-of-order block, heartbeat lag) + SYNC_BATCH_MAX=256/15s/one-in-flight constraints. Three real node roles via CLI flags (Founder Validator, Follower `--follower`, Public RPC `--rpc 0.0.0.0:8545`). Port table — REAL ports only: 30333/TCP libp2p public on all nodes, 8545/TCP JSON-RPC (CLI default `0.0.0.0` — recommended hardening to `127.0.0.1` on validator), 5353/UDP mDNS LAN-only. Four start-command recipes (founder, follower, public RPC, single-node `--no-p2p` legacy). Multiaddr format `/ip4/<ip>/tcp/<port>/p2p/<peer_id>` with grep recipe to find peer-id from `🌐 p2p listening` log line + warning that peer-id rotates per restart (pinning via `with_existing_identity` is on Phase B hardening list). Verify section with INFO-level grep recipe (🌐/🔗/✅/📦/⏬) PLUS optional `RUST_LOG=zebvix_node=debug` for DEBUG-level 📥/📤 visibility, plus tip-height comparison RPC across two hosts. Replaces earlier Sui-template page that referenced `fullnode.yaml`, `genesis.blob`, `/ip4/.../tcp/8080/http` (libp2p doesn't use `/http`), and fictional ports 8080/9000/9001/9184. Two architect rounds: Round 1 caught votes-mislabeled-as-future + RPC default misframe + peer-id-rotation wording + debug-vs-info log mismatch — all 4 fixed in Round 2 (PASS).
- **Genesis Configuration:** Anatomy + customization guide for the chain bootstrap document. Hero (chain_id 7878 / ZBX 18 decimals / 5 sec blocks / 1 deterministic genesis validator); literal anatomy table of `struct GenesisFile` from `main.rs:765` (chain_id u64, chain_name String, token_symbol String, decimals u32, max_supply_wei String, block_time_secs u64, validator_address String, alloc Vec<(String, String)>, timestamp i64) with default values from tokenomics.rs; explanation of the deterministic-founder-validator design (every node binds the same `FOUNDER_PUBKEY_HEX`-derived validator at genesis to eliminate divergence); real srv1266996 `genesis.json` example with founder addr `0x40907000…0c0315` + 9.99M ZBX premine + warning that joiners must copy genesis verbatim; alloc customization (`--alloc addr:amount_zbx` repeatable, `--no-default-premine` skip) with 3-tile supply breakdown (founder 9.99M + AMM seed 20M + mineable 120.01M = 150M cap); `node.json` sidecar explanation (per-node, must NOT be copied); inspect & verify with `cat genesis.json | jq` + `eth_chainId` + `zbx_listValidators` + `zbx_supply`. Documents (with explicit footnote) the stale `cmd_init` log message that says `validator-add` is "admin-signed" when actual on-chain enforcement requires the **current governor** — to be reworded in next chain release. Replaces earlier Sui-template page that referenced `genesis.blob`, `genesis.yaml`, Move framework, and mist (10⁹) units.
- **Environment Setup:** Operator-facing build/run guide for VPS deployment. Hero with codebase/network/build-time facts; hardware (4 vCPU / 8 GB / 100 GB SSD / 100 Mbps) and OS (Ubuntu 22.04+/24.04, Debian 12, glibc Linux only — no Alpine/musl) recommendations; 9-step flow: (1) Rust stable via rustup, (2) native deps `build-essential pkg-config cmake libssl-dev libclang-dev clang llvm-dev libsnappy-dev liblz4-dev zlib1g-dev libzstd-dev` with per-package rationale, (3) source tarball pull from `/api/download/newchain` with backup-before-extract guard, (4) `cargo build --release` (or `--features evm` for native EVM), (5) symlink to `/usr/local/bin/zebvix-node`, (6) two-step init (`keygen --out` then `init --home --validator-key`) with founder vs follower note, (7) production systemd unit verbatim from `srv1266996` (`After=network.target`, `Restart=always`, `RestartSec=5`, journal-only stdout/err) + optional hardening labeled separately, (8) firewall split into validator-locked-down profile (`--rpc 127.0.0.1:8545` + ufw deny 8545) and public-RPC profile (`--rpc 0.0.0.0:8545` + nginx/TLS), (9) RPC sanity (`eth_chainId=0x1ec6`, `zbx_blockNumber`, `zbx_getStaking`, `zbx_supply`); day-2 update flow + troubleshooting (libclang, no blocks produced, RPC connection refused, disk fill). Replaced earlier Sui-fork template that incorrectly told operators to `git clone MystenLabs/sui` and `cargo build -p sui-node`.
- **Validator Setup:** Live `zbx_listValidators` + `zbx_getStaking` every 10s; hero stats (active vals/total bonded/current epoch/epoch reward 50 ZBX/quorum); active validator table with copyable addresses; network params (min self-bond 100 ZBX fixed, min delegation 10 ZBX, max commission 50%, max edit ±1%/epoch, epoch 17280 blocks ~24h, unbonding 7 epochs ~7d); 6-step setup guide (secp256k1 keygen → init w/ `--validator-key` flag → systemd → firewall/sync → two-tier registration: `StakeOp::CreateValidator` 5a + governor-only `ValidatorAdd` 5b → verify); reward economics with bootstrap-APY caveat; slashing primitives banner (`slash_double_sign` 5%, `slash_downtime` 0.10% — auto-enforcement ships later); RPC cheatsheet.
- **Staking constants & redelegate-inflation fix (Apr 2026):** `MIN_SELF_BOND_WEI` set to 100 ZBX (fixed token amount), `MIN_DELEGATION_WEI` set to 10 ZBX (fixed). Architect caught a pre-existing inflation vector in `StakingModule::redelegate` — old code did `self.stake(.., amount.max(MIN_DELEGATION_WEI))` which minted free stake when a sub-min position was redelegated. Refactored: `stake()` checks min then calls new private `deposit_unchecked()`, and `redelegate()` calls `deposit_unchecked()` directly with the exact moved amount. Regression test `redelegate_legacy_small_position_does_not_inflate` added.
- **USD-peg removed (Apr 2026):** Earlier design derived validator min from `MIN_SELF_BOND_USD_MICRO = $50` ÷ AMM pool spot price. Removed entirely — the chain's own AMM was the oracle (reflexive, shallow, flash-loan-manipulable: an attacker could pump spot price to lower the ZBX min and spawn cheap Sybil validators). Replaced with fixed-token `MIN_SELF_BOND_WEI = 100 ZBX`, matching industry standard (Ethereum 32 ETH, Solana ~5,000 SOL, Sui 30M SUI, Aptos 1M APT). Deleted `MIN_SELF_BOND_USD_MICRO` constant and `dynamic_min_self_bond_wei()` helper from `staking.rs`. `state.rs` calls `MIN_SELF_BOND_WEI` directly. `rpc.rs::zbx_getStaking` no longer emits `min_self_bond_usd_micro` or `min_self_bond_dynamic_wei`. Dashboard `validators.tsx` removed the "USD-pegged" row and updated the hint to explain the fixed-token design + industry comparison. Future USD-aware logic, if reintroduced, must use external TWAP oracle (Chainlink/Pyth) and remain `max(MIN_SELF_BOND_WEI, oracle_value)`. Requires VPS rebuild + node restart to activate.
- **Mobile Wallet UI:** Flutter application with onboarding (BIP39 mnemonic), wallet management, swap functionality, multisig, and QR-based desktop pairing.

## Deployment & Operations
- VPS topology defined for Node-1 (founder/proposer) and Node-2 (follower).
- CLI tools for chain interaction, including validator management, pool genesis, and bridge operations.

# External Dependencies

- **Monorepo:** pnpm workspaces
- **Blockchain Core:** Rust, `ed25519-dalek` (removed), `k256` (for secp256k1), RocksDB
- **Web Server:** Express 5
- **Database:** PostgreSQL, Drizzle ORM
- **Validation:** Zod
- **API Codegen:** Orval
- **Build Tool:** esbuild
- **Cryptography (JS):** `@noble/curves/secp256k1`
- **Mobile Development:** Flutter, `flutter_secure_storage`
- **EVM (Rust):** `sha2` (optional feature), `tempfile` (dev-dependency)
- **Smart Contracts:** Solidity 0.8.24 (Hardhat/Foundry for compilation)
- **Client Libraries (Dashboard):** ethers.js (for EVM integration)