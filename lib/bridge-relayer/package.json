{
  "name": "@workspace/bridge-relayer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Cross-chain relayer between Zebvix L1 and BNB Smart Chain. Watches Zebvix BridgeOut events, collects M-of-N validator signatures, submits aggregated mintFromZebvix txs on BSC. Watches BSC BurnToZebvix events, submits zbx_submitBridgeIn on Zebvix.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@noble/curves": "^2.2.0",
    "@noble/hashes": "^2.2.0",
    "better-sqlite3": "^11.6.0",
    "ethers": "^6.16.0",
    "express": "^5",
    "pino": "^9",
    "pino-pretty": "^13",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/express": "^5.0.6",
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "^5.7.0"
  }
}
